version: "2"

linters:
  default: all
  disable:
    - cyclop
    - depguard
    - exhaustruct
    - exhaustruct_v5
    - funlen
    - gochecknoglobals
    - gocognit
    - goconst
    - godox
    - gomodguard
    - lll
    - mnd
    - nlreturn
    - noinlineerr
    - testpackage
    - varnamelen
    - wrapcheck
    - wsl
    - wsl_v5
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
    tagliatelle:
      case:
        rules:
          json: snake
          yaml: snake
  exclusions:
    rules:
      - path: "keymap.go"
        linters:
          - godoclint
formatters:
  enable:
    - gofumpt
    - goimports
